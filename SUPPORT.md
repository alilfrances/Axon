# Support

Use GitHub Issues for reproducible bugs and focused feature requests.

Before opening an issue, run:

```bash
axon doctor
python -m pytest
```

Include the Axon version, Python version, operating system, MCP host, and whether optional tools such as Semgrep or Cortex are installed.

Report suspected vulnerabilities privately through `SECURITY.md`.
